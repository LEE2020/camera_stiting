bool pc::CylinderProjector::mapBackward(int u, int v, double& x, double& y) const {
    // ... (你的 mapBackward 函数的实现) ...

    // 添加调试信息
    std::cout << "mapBackward: u=" << u << ", v=" << v << std::endl;
    std::cout << "mapBackward: x=" << x << ", y=" << y << std::endl;

    if (/* 你的有效性检查 */) {
        // ...
        return true;
    } else {
        std::cout << "mapBackward: FAILED" << std::endl;
        return false;
    }
}
